function nowPlaying(input) {
    let trackName = input[0];
    let artistName = input[1];
    let trackLength = input[2];

    console.log(`Now Playing: ${artistName} - ${trackName} [${trackLength}]`);
}